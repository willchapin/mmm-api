//import { createPurchaseSchema, getPurchaseSchema, updatePurchaseSchema } from './schema';
//import { validateParams } from '../validation-util';
//
//export async function validateGetUserById(ctx, next) {
//  await validateParams(ctx.params, getPurchaseSchema, ctx, next);
//}
//
//export async function validateCreatePurchase(ctx, next) {
//  await validateParams(ctx.request.body, createPurchaseSchema, ctx, next);
//}
//
//export async function validateUpdatePurchase(ctx, next) {
//  await validateParams(ctx.request.body, updatePurchaseSchema, ctx, next);
//}
